import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    userText: {
        color: '#d1d1d1',
        fontSize: 18,
        marginVertical: 10
    }
})

export default styles;