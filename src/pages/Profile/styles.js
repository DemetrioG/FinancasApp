import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    nameText: {
        color: '#d1d1d1',
        fontSize: 24,
        fontWeight: 'bold',
        marginVertical: 30,
    },

    btnText: {
        fontSize: 18,
        textAlign: 'center',
        fontWeight: 'bold',
        color: '#FFF'
    }
})

export default styles;