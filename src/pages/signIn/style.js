import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    image: {
        marginBottom: 40,
    },

    textLink: {
        color: '#d1d1d1',
    },
});

export default styles;