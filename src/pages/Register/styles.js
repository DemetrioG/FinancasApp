import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    btnText: {
        fontSize: 18,
        textAlign: 'center',
        color: '#FFF',
        fontWeight: 'bold'
    }
})

export default styles;